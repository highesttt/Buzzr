<UserControl
    x:Class="Buzzr.Views.ChatListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Dark">

    <UserControl.Resources>
        <Style x:Key="NoFlickerButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A1A1A1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="1"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="1"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="0.8"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="{TemplateBinding Padding}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}">
                                <ContentPresenter
                                    Content="{TemplateBinding Content}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <Border x:Name="HoverOverlay"
                                    Background="#15FFFFFF"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Opacity="0" IsHitTestVisible="False"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,48,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Chats" FontSize="20" FontWeight="SemiBold"
                       Foreground="#F3F3F3" FontFamily="Segoe UI"/>
            <Button x:Name="NewChatBtn" Grid.Column="1"
                    Width="28" Height="28" Padding="0"
                    Background="Transparent" BorderThickness="0"
                    ToolTipService.ToolTip="New chat (Ctrl+Shift+N)">
                <FontIcon Glyph="&#xE710;" FontSize="14" Foreground="{StaticResource AccentBrush}"/>
            </Button>
        </Grid>

        <TextBox x:Name="SearchBox" Grid.Row="1"
                 PlaceholderText="Search chats... (Ctrl+K)"
                 Margin="8,0,8,4"/>

        <StackPanel x:Name="TabBar" Grid.Row="2" Orientation="Horizontal"
                    Spacing="0" Margin="8,4,8,8">
            <Button x:Name="TabAll" Content="All" Style="{StaticResource NoFlickerButton}"
                    Background="#2D2D2D" Foreground="#F3F3F3" CornerRadius="4,0,0,4"/>
            <Button x:Name="TabUnread" Content="Unread" Style="{StaticResource NoFlickerButton}"
                    CornerRadius="0"/>
            <Button x:Name="TabPinned" Content="Pinned" Style="{StaticResource NoFlickerButton}"
                    CornerRadius="0"/>
            <Button x:Name="TabArchived" Content="Archived" Style="{StaticResource NoFlickerButton}"
                    CornerRadius="0"/>
            <Button x:Name="TabLowPriority" Content="Low Priority" Style="{StaticResource NoFlickerButton}"
                    CornerRadius="0,4,4,0"/>
        </StackPanel>

        <ScrollViewer x:Name="ChatScroll" Grid.Row="3" Padding="4,0,4,4">
            <StackPanel x:Name="ListStack" Spacing="1"/>
        </ScrollViewer>

        <StackPanel x:Name="LoadingPanel" Grid.Row="3"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Spacing="12">
            <ProgressRing x:Name="LoadingRing" IsActive="True"
                          Width="32" Height="32"/>
            <TextBlock x:Name="LoadingText" Text="Loading chats..."
                       FontSize="13" Foreground="{StaticResource AccentBrush}" FontFamily="Segoe UI"
                       HorizontalAlignment="Center" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
