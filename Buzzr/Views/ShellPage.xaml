<Page
    x:Class="Buzzr.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Buzzr.Views"
    RequestedTheme="Dark"
    Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="44"/>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" BorderBrush="#1A1E20" BorderThickness="0,0,1,0">
            <Grid Background="#E6242424">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title bar spacer -->
                <Border Grid.Row="0" Height="44"/>

                <!-- All chats icon -->
                <Button x:Name="AllChatsBtn" Grid.Row="1"
                        Width="36" Height="36" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Center" Margin="0,4,0,14"
                        ToolTipService.ToolTip="All chats">
                    <FontIcon Glyph="&#xE8BD;" FontSize="16" Foreground="#4CC2FF"/>
                </Button>

                <!-- Account icons -->
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalAlignment="Center"
                              Padding="0,12,0,4">
                    <StackPanel x:Name="AccountIconsStack" Spacing="4"/>
                </ScrollViewer>

                <!-- Settings gear -->
                <Button x:Name="SettingsBtn" Grid.Row="3"
                        Width="36" Height="36" Padding="0"
                        Background="Transparent" BorderThickness="0"
                        HorizontalAlignment="Center" Margin="0,0,0,8">
                    <FontIcon Glyph="&#xE713;" FontSize="16" Foreground="#A1A1A1"/>
                    <Button.Flyout>
                        <MenuFlyout x:Name="SettingsFlyout" Placement="RightEdgeAlignedBottom">
                            <MenuFlyoutItem x:Name="UserNameItem" IsEnabled="False"/>
                            <MenuFlyoutItem x:Name="AccountCountItem" IsEnabled="False"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem x:Name="SettingsItem" Text="Settings">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE713;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem x:Name="DisconnectItem" Text="Disconnect"
                                            Foreground="#FF4343">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE7E8;" Foreground="#FF4343"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem x:Name="AboutItem" Text="About Buzzr">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE946;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </Grid>
        </Border>

        <!-- Chat list -->
        <Border Grid.Column="1" BorderBrush="#1A1E20" BorderThickness="0,0,1,0"
                Background="#E6242424">
            <local:ChatListControl x:Name="ChatList"/>
        </Border>

        <!-- Messages -->
        <local:MessageView x:Name="MessagePanel" Grid.Column="2"/>
    </Grid>
</Page>
